services:
  frontend:
    container_name: refund-frontend
    build:
      context: .
      dockerfile: Dockerfile
      args:
        VITE_APP_API_URL: ${VITE_APP_API_URL}
        VITE_APP_SCHOLARSDEN_API_URL: ${VITE_APP_SCHOLARSDEN_API_URL}
    ports:
      - "80:80"
    restart: unless-stopped
